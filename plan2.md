# UI/UX 개선 계획서

## 개요

**프로젝트**: 쌤찾기 (SellmeBuyme) - 교육 인력 매칭 플랫폼
**분석 일자**: 2026-01-18
**목적**: 사용자 경험 향상을 위한 UI/UX 개선 방안 수립

---

## 1. 현재 상태 분석

### 1.1 전체 아키텍처

```
[현재 레이아웃 구조]

Desktop (≥768px):
┌─────────────────────────────────────────────────────────┐
│  Header (Logo + Filters + Search + Auth)                │
├─────────────────────────────────────────────────────────┤
│  Promo Cards (좌측)  │  Kakao Map (우측, 전체 높이)      │
│  ─────────────────  │                                   │
│  Job Cards List     │                                   │
│  (스크롤 가능)       │                                   │
└─────────────────────────────────────────────────────────┘

Mobile (<768px):
┌─────────────────────┐
│  Mobile Header      │
├─────────────────────┤
│  Promo Section      │
├─────────────────────┤
│  Cards List         │
│  (무한 스크롤)       │
├─────────────────────┤
│  Bottom Nav         │
└─────────────────────┘
```

### 1.2 컴포넌트 현황

| 카테고리 | 컴포넌트 수 | 주요 컴포넌트 |
|---------|------------|--------------|
| Layout | 8개 | Header, MobileHeader, MobileBottomNav |
| Cards | 12개 | JobCard, TalentCard, CompactJobCard |
| Auth | 6개 | ProfileSetupModal, SocialSignupModal |
| AI | 4개 | AIRecommendations, AIInsightBox |
| Mobile | 10개 | MobileProfilePage, RegisterBottomSheet |
| Map | 3개 | MapPopup, KakaoMap |
| Admin | 15개+ | 크롤러 관리, 건강 체크 |

### 1.3 현재 디자인 시스템

**색상 팔레트**:
- Primary (공고): `#a8c5e0` → `#7aa3cc` → `#68B2FF`
- Talent (인력): `#c5e3d8` → `#7db8a3` → `#6fb59b`
- Experience (체험): `#ffd98e` → `#f4c96b`
- Background: White/Gray gradients

**타이포그래피**:
- 폰트: esamanru (Korean custom font)
- 제목: `text-lg font-extrabold`
- 본문: `text-sm`, `text-base`

**아이콘**:
- Tabler Icons (주로 사용)
- Lucide React (보조)

---

## 2. 발견된 UX 문제점

### 2.1 🔴 Critical Issues

#### P1. 빈 상태(Empty State) UX 부재
**현상**: 공고가 없을 때 "표시할 공고가 없습니다" 단순 텍스트만 표시
**영향**: 사용자가 다음 행동을 알 수 없음, 이탈률 증가
**개선안**:
- 일러스트레이션과 함께 친근한 메시지 표시
- "다른 지역 검색하기", "필터 초기화" 등 CTA 버튼 제공
- 추천 검색어 또는 인기 지역 표시

#### P2. 검색 결과 피드백 부족
**현상**: 검색 후 결과 개수만 표시 (예: "30개")
**영향**: 검색어와 결과의 관련성 파악 어려움
**개선안**:
- "서울 지역 기간제교사 공고 30개" 형태로 컨텍스트 제공
- 검색어 하이라이팅
- "검색어 OOO에 대한 결과가 없습니다" + 유사 검색어 제안

#### P3. 모바일 하단 네비게이션 가독성
**현상**: 하단 네비게이션 버튼 레이블이 잘려서 표시
**영향**: 기능 인지 어려움, 탭 오류 가능성
**개선안**:
- 아이콘 + 짧은 레이블로 재설계
- 현재 선택된 탭 시각적 강조

### 2.2 🟡 Important Issues

#### P4. 카드 정보 밀도 불균형
**현상**: JobCard에 많은 정보 압축 (기관명, 제목, 태그, 위치, 보수, 마감일)
**영향**: 시각적 피로, 중요 정보 식별 어려움
**개선안**:
- 정보 계층 재설계 (Primary/Secondary/Tertiary)
- 호버/탭 시 상세 정보 노출 (Progressive Disclosure)
- 카드 높이 약간 증가하여 여백 확보

#### P5. 필터 UI 개선 필요
**현상**: 학교급, 과목 드롭다운만 존재, 복합 필터 어려움
**영향**: 원하는 공고 찾기 어려움
**개선안**:
- 필터 태그 시스템 도입 (선택된 필터 시각화)
- 급여 범위, 마감 임박 필터 추가
- 필터 초기화 버튼

#### P6. 상세 모달 레이아웃
**현상**: 모달이 중앙 정렬, 지도와 분리됨
**영향**: 위치 확인을 위해 모달 닫아야 함
**개선안**:
- 슬라이드-인 패널 형태로 변경 (지도 옆에 표시)
- 모달 내 미니맵 포함
- 스와이프로 다음 공고 이동

#### P7. 로딩 상태 시각화 부족
**현상**: 데이터 로딩 시 명확한 피드백 없음
**영향**: 사용자가 앱이 멈춘 것으로 인식
**개선안**:
- 스켈레톤 로딩 UI 적용
- 지도 로딩 시 프로그레스 표시
- 무한 스크롤 로딩 인디케이터

### 2.3 🟢 Enhancement Opportunities

#### P8. 마이크로 인터랙션 부족
**현상**: 버튼, 카드 클릭 시 기본 트랜지션만 적용
**영향**: 앱의 반응성과 품질 인식 저하
**개선안**:
- 북마크 하트 애니메이션
- 카드 호버 시 미세한 확대/그림자
- 탭 전환 시 슬라이드 애니메이션

#### P9. 접근성(A11y) 개선
**현상**: 일부 버튼 aria-label 누락, 키보드 네비게이션 제한
**영향**: 스크린 리더 사용자 접근 제한
**개선안**:
- 모든 인터랙티브 요소에 aria-label 추가
- 키보드 포커스 스타일 개선
- 색상 대비 검증 (WCAG AA 기준)

#### P10. 온보딩 경험
**현상**: 첫 방문 사용자를 위한 가이드 없음
**영향**: 핵심 기능 발견 지연
**개선안**:
- 첫 방문 시 간단한 투어 제공
- 툴팁으로 주요 기능 안내
- 프로필 설정 유도 모달

---

## 3. 개선 우선순위 매트릭스

```
영향도 높음 │ P1, P2, P3    │ P6, P10
           │ (즉시 수정)    │ (단기 계획)
───────────┼───────────────┼───────────────
영향도 낮음 │ P4, P5, P7    │ P8, P9
           │ (중기 계획)    │ (장기 개선)
───────────┴───────────────┴───────────────
             노력 적음         노력 많음
```

---

## 4. 상세 개선 계획

### Phase 1: Critical Fixes (1-2주)

#### 4.1.1 빈 상태 UI 개선
**파일**: `src/components/cards/CardGrid.tsx`

```tsx
// 개선 전
<div>표시할 공고가 없습니다</div>

// 개선 후
<EmptyState
  illustration={<SearchIllustration />}
  title="검색 결과가 없어요"
  description="다른 지역이나 조건으로 검색해 보세요"
  actions={[
    { label: "필터 초기화", onClick: resetFilters },
    { label: "인기 지역 보기", onClick: showPopularRegions }
  ]}
/>
```

#### 4.1.2 검색 결과 컨텍스트
**파일**: `src/components/layout/Header.tsx`

```tsx
// 검색 결과 요약 표시
<SearchResultSummary
  query={searchQuery}
  count={totalCount}
  filters={activeFilters}
/>
// 출력: "서울 · 기간제교사 검색 결과 30개"
```

#### 4.1.3 모바일 하단 네비게이션 개선
**파일**: `src/components/mobile/MobileBottomNav.tsx`

```tsx
// 아이콘 + 축약 레이블
const navItems = [
  { icon: <IconBriefcase />, label: "공고", active: true },
  { icon: <IconUser />, label: "인재", active: false },
  { icon: <IconSchool />, label: "체험", active: false },
  { icon: <IconPlus />, label: "등록", active: false },
  { icon: <IconHeart />, label: "저장", active: false }
];
```

### Phase 2: Important Improvements (2-4주)

#### 4.2.1 카드 정보 계층 재설계
**파일**: `src/components/cards/JobCard.tsx`

**정보 계층 구조**:
```
[Level 1 - 즉시 인지]
├── 기관명 (Bold, Large)
├── 마감일 D-day 배지 (색상 강조)

[Level 2 - 스캔 가능]
├── 직종 태그
├── 위치
├── 급여 (요약)

[Level 3 - 호버/탭 시 노출]
├── 상세 급여 정보
├── 근무 기간
├── 접수 기간
└── 연락처
```

#### 4.2.2 필터 시스템 강화
**새 컴포넌트**: `src/components/filters/FilterBar.tsx`

```tsx
interface FilterBarProps {
  filters: {
    schoolLevel: string[];
    subject: string[];
    salaryRange: [number, number];
    deadline: 'all' | 'week' | 'month';
  };
  onChange: (filters: Filters) => void;
}

// 선택된 필터를 태그로 표시
<FilterTags filters={activeFilters} onRemove={removeFilter} />
```

#### 4.2.3 스켈레톤 로딩 UI
**새 컴포넌트**: `src/components/common/Skeleton.tsx`

```tsx
// 카드 스켈레톤
<CardSkeleton count={6} />

// 지도 스켈레톤
<MapSkeleton />
```

### Phase 3: Enhancements (4-8주)

#### 4.3.1 마이크로 인터랙션
**라이브러리**: Framer Motion (이미 설치됨)

```tsx
// 북마크 애니메이션
<motion.button
  whileTap={{ scale: 0.9 }}
  animate={{
    scale: bookmarked ? [1, 1.2, 1] : 1,
    color: bookmarked ? "#ef4444" : "#d1d5db"
  }}
>
  <IconHeart />
</motion.button>
```

#### 4.3.2 접근성 개선
**체크리스트**:
- [ ] 모든 이미지에 alt 속성
- [ ] 버튼에 aria-label
- [ ] 포커스 아웃라인 스타일
- [ ] 색상 대비 4.5:1 이상
- [ ] 키보드 네비게이션 테스트

#### 4.3.3 온보딩 투어
**새 컴포넌트**: `src/components/tour/WelcomeTour.tsx`

```tsx
const tourSteps = [
  { target: '.search-input', content: '검색어를 입력하세요' },
  { target: '.filter-buttons', content: '필터로 원하는 조건을 설정하세요' },
  { target: '.job-card', content: '카드를 클릭하면 상세 정보를 볼 수 있어요' },
  { target: '.bookmark-btn', content: '관심 공고는 북마크하세요' }
];
```

---

## 5. 새 컴포넌트 설계

### 5.1 EmptyState 컴포넌트

**위치**: `src/components/common/EmptyState.tsx`

```tsx
interface EmptyStateProps {
  illustration?: React.ReactNode;
  title: string;
  description?: string;
  actions?: Array<{
    label: string;
    onClick: () => void;
    variant?: 'primary' | 'secondary';
  }>;
}
```

### 5.2 SearchResultSummary 컴포넌트

**위치**: `src/components/search/SearchResultSummary.tsx`

```tsx
interface SearchResultSummaryProps {
  query?: string;
  count: number;
  filters?: {
    schoolLevel?: string;
    subject?: string;
    region?: string;
  };
}
```

### 5.3 FilterTags 컴포넌트

**위치**: `src/components/filters/FilterTags.tsx`

```tsx
interface FilterTagsProps {
  filters: Array<{
    key: string;
    label: string;
    value: string;
  }>;
  onRemove: (key: string) => void;
  onClearAll: () => void;
}
```

### 5.4 CardSkeleton 컴포넌트

**위치**: `src/components/common/CardSkeleton.tsx`

```tsx
interface CardSkeletonProps {
  count?: number;
  type?: 'job' | 'talent' | 'experience';
}
```

---

## 6. 디자인 토큰 정리

### 6.1 색상 시스템 통합

```css
:root {
  /* Primary - Job Postings */
  --color-job-50: #f0f7ff;
  --color-job-100: #e0efff;
  --color-job-500: #68b2ff;
  --color-job-600: #4a9de8;

  /* Secondary - Talent */
  --color-talent-50: #f0fdf4;
  --color-talent-100: #dcfce7;
  --color-talent-500: #6fb59b;

  /* Tertiary - Experience */
  --color-experience-50: #fffbeb;
  --color-experience-500: #f4c96b;

  /* Semantic Colors */
  --color-urgent: #ef4444;
  --color-success: #22c55e;
  --color-warning: #f59e0b;
  --color-info: #3b82f6;

  /* Neutrals */
  --color-gray-50: #f9fafb;
  --color-gray-100: #f3f4f6;
  --color-gray-500: #6b7280;
  --color-gray-900: #111827;
}
```

### 6.2 간격 시스템

```css
:root {
  --space-1: 0.25rem;  /* 4px */
  --space-2: 0.5rem;   /* 8px */
  --space-3: 0.75rem;  /* 12px */
  --space-4: 1rem;     /* 16px */
  --space-6: 1.5rem;   /* 24px */
  --space-8: 2rem;     /* 32px */
}
```

### 6.3 타이포그래피

```css
:root {
  /* Font Sizes */
  --text-xs: 0.75rem;    /* 12px */
  --text-sm: 0.875rem;   /* 14px */
  --text-base: 1rem;     /* 16px */
  --text-lg: 1.125rem;   /* 18px */
  --text-xl: 1.25rem;    /* 20px */

  /* Font Weights */
  --font-normal: 400;
  --font-medium: 500;
  --font-semibold: 600;
  --font-bold: 700;
  --font-extrabold: 800;
}
```

---

## 7. 측정 지표 (KPIs)

### 7.1 사용성 지표

| 지표 | 현재 | 목표 | 측정 방법 |
|-----|------|------|----------|
| 첫 의미있는 액션까지 시간 | 측정 필요 | < 10초 | Analytics |
| 검색 후 클릭률 | 측정 필요 | > 30% | Event tracking |
| 빈 상태 이탈률 | 측정 필요 | < 20% | Bounce rate |
| 모바일 네비게이션 오류율 | 측정 필요 | < 5% | Error logging |

### 7.2 성능 지표

| 지표 | 현재 | 목표 |
|-----|------|------|
| LCP (Largest Contentful Paint) | 측정 필요 | < 2.5s |
| FID (First Input Delay) | 측정 필요 | < 100ms |
| CLS (Cumulative Layout Shift) | 측정 필요 | < 0.1 |

---

## 8. 구현 타임라인

```
Week 1-2: Phase 1 (Critical)
├── EmptyState 컴포넌트 구현
├── SearchResultSummary 구현
└── MobileBottomNav 개선

Week 3-4: Phase 2 (Important)
├── 카드 정보 계층 재설계
├── FilterTags 시스템
└── 스켈레톤 로딩 UI

Week 5-6: Phase 2 (계속)
├── 상세 모달 레이아웃 개선
└── 필터 확장 기능

Week 7-8: Phase 3 (Enhancement)
├── 마이크로 인터랙션
├── 접근성 개선
└── 온보딩 투어
```

---

## 9. 참고 리소스

### 디자인 레퍼런스
- [사람인](https://www.saramin.co.kr) - 채용 플랫폼 UI
- [잡코리아](https://www.jobkorea.co.kr) - 검색/필터 UX
- [원티드](https://www.wanted.co.kr) - 카드 UI/모바일 UX

### 컴포넌트 라이브러리
- [21st.dev](https://21st.dev) - Magic MCP 활용
- [shadcn/ui](https://ui.shadcn.com) - 컴포넌트 패턴

### 접근성 가이드
- [WCAG 2.1](https://www.w3.org/WAI/WCAG21/quickref/)
- [MDN Accessibility](https://developer.mozilla.org/en-US/docs/Web/Accessibility)

---

## 10. 결론

현재 쌤찾기 플랫폼은 핵심 기능이 잘 구현되어 있으나, 사용자 경험 측면에서 개선 여지가 있습니다. 특히:

1. **빈 상태 및 에러 상태** 처리가 미흡하여 사용자 이탈 위험
2. **검색/필터 피드백** 부족으로 탐색 경험 저하
3. **모바일 네비게이션** 가독성 문제

위 계획을 단계적으로 적용하면 사용자 만족도와 전환율을 크게 개선할 수 있을 것입니다.

---

**작성자**: Claude Code
**버전**: 1.0
**최종 수정**: 2026-01-18
