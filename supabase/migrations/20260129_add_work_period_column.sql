-- ============================================================================
-- 공고 테이블에 근무기간(work_period) 컬럼 추가
-- 작성일: 2026-01-29
-- 설명: 직접 등록 공고의 근무기간을 별도 필드로 관리
-- ============================================================================

-- 근무기간 컬럼 추가
ALTER TABLE public.job_postings
ADD COLUMN IF NOT EXISTS work_period TEXT;

-- 컬럼 코멘트
COMMENT ON COLUMN public.job_postings.work_period IS '근무기간 (예: 2026.2.24~2026.2.27)';
