# GitHub Actions - 배포 추적 (수정 버전)
name: Track Deployment

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  track:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Track deployment to Supabase
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          # 직접 Supabase REST API로 삽입 (Edge Function 우회)
          echo "🚀 배포 기록 중..."

          # HTTP 응답 코드와 함께 curl 실행
          RESPONSE=$(curl -w "\n%{http_code}" -X POST "$SUPABASE_URL/rest/v1/github_deployments" \
            -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" \
            -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY" \
            -H "Content-Type: application/json" \
            -H "Prefer: return=representation" \
            -d "{
              \"commit_sha\": \"${{ github.sha }}\",
              \"commit_message\": $(echo '${{ github.event.head_commit.message }}' | jq -Rs .),
              \"branch\": \"${{ github.ref_name }}\",
              \"author\": \"${{ github.event.head_commit.author.name }}\",
              \"status\": \"success\",
              \"workflow_run_id\": \"${{ github.run_id }}\",
              \"deployed_at\": \"${{ github.event.head_commit.timestamp }}\"
            }")

          # HTTP 상태 코드 추출 (마지막 줄)
          HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
          BODY=$(echo "$RESPONSE" | head -n-1)

          echo "HTTP 상태 코드: $HTTP_CODE"

          # 200-299 범위는 성공
          if [ "$HTTP_CODE" -ge 200 ] && [ "$HTTP_CODE" -lt 300 ]; then
            echo "✅ 배포 기록 성공!"
            exit 0
          else
            echo "⚠️ 배포 기록 실패 (HTTP $HTTP_CODE)"
            echo "응답: $BODY"
            exit 1
          fi

      - name: Purge Cloudflare Cache
        if: success()
        env:
          CF_ZONE_ID: ${{ secrets.CF_ZONE_ID }}
          CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
        run: |
          if [ -z "$CF_ZONE_ID" ] || [ -z "$CF_API_TOKEN" ]; then
            echo "⚠️ Cloudflare secrets 미설정 - 캐시 퍼지 건너뜀"
            echo "설정 방법: GitHub Repo → Settings → Secrets → CF_ZONE_ID, CF_API_TOKEN 추가"
            exit 0
          fi

          echo "🧹 Cloudflare 캐시 퍼지 중..."
          RESPONSE=$(curl -s -w "\n%{http_code}" -X POST \
            "https://api.cloudflare.com/client/v4/zones/$CF_ZONE_ID/purge_cache" \
            -H "Authorization: Bearer $CF_API_TOKEN" \
            -H "Content-Type: application/json" \
            --data '{"purge_everything":true}')

          HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
          BODY=$(echo "$RESPONSE" | head -n-1)

          if [ "$HTTP_CODE" -eq 200 ]; then
            echo "✅ Cloudflare 캐시 퍼지 완료! 사용자들이 즉시 새 버전을 볼 수 있습니다."
          else
            echo "⚠️ 캐시 퍼지 실패 (HTTP $HTTP_CODE)"
            echo "응답: $BODY"
            # 캐시 퍼지 실패해도 배포는 성공이므로 exit 0
            exit 0
          fi
