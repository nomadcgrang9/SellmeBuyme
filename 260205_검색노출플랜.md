# 🔍 260205 검색노출 개선 플랜

> **작성일:** 2026-02-05  
> **목표:** 학교일자리.com의 구글/네이버 검색 상위 노출

---

## 📊 현재 상태 진단

### ✅ 이미 완료된 것

| 항목 | 상태 | 파일 위치 |
|------|------|-----------|
| robots.txt | ✅ 완료 | `public/robots.txt` |
| sitemap.xml | ✅ 완료 | `public/sitemap.xml` |
| 기본 메타 태그 | ✅ 완료 | `index.html` |
| Open Graph 태그 | ✅ 완료 | `index.html` |
| Twitter Card | ✅ 완료 | `index.html` |
| JSON-LD 구조화 데이터 | ✅ 완료 | `index.html` (WebSite 스키마) |
| Canonical URL | ✅ 완료 | `index.html` |

### ⚠️ 부족한 것 (개선 필요)

| 항목 | 현재 상태 | 필요 조치 |
|------|-----------|-----------|
| 동적 메타 태그 | ❌ 없음 | 각 페이지별 고유 메타 태그 필요 |
| 지역별 랜딩 페이지 | ❌ 없음 | SEO용 정적 페이지 필요 |
| 개별 공고 SEO | ❌ 없음 | 채용공고별 메타/구조화 데이터 |
| sitemap 자동 생성 | ❌ 수동 | 동적 sitemap 필요 |
| 네이버 서치어드바이저 | ❓ 미확인 | 등록 확인 필요 |
| 구글 서치 콘솔 | ❓ 미확인 | 등록 확인 필요 |

---

## 🎯 개선 로드맵

### Phase 1: 즉시 실행 (1주 이내) ⚡

#### 1-1. 검색 엔진 등록 확인/실행
```
┌─────────────────────────────────────────────────────────┐
│  구글 서치 콘솔 등록                                      │
│  https://search.google.com/search-console               │
│                                                         │
│  1. 속성 추가 → https://학교일자리.com                    │
│  2. 소유권 확인 (HTML 파일 또는 DNS)                      │
│  3. sitemap.xml 제출                                    │
│  4. 색인 생성 요청                                       │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  네이버 서치어드바이저 등록                               │
│  https://searchadvisor.naver.com                        │
│                                                         │
│  1. 사이트 추가 → https://학교일자리.com                  │
│  2. 소유권 확인                                          │
│  3. sitemap.xml 제출                                    │
│  4. 웹 페이지 수집 요청                                  │
└─────────────────────────────────────────────────────────┘
```

#### 1-2. sitemap.xml 날짜 업데이트
현재 `lastmod`가 `2026-01-31`로 고정 → **오늘 날짜로 업데이트 필요**

---

### Phase 2: 핵심 개선 (2주 이내) 🔧

#### 2-1. 동적 메타 태그 구현 (react-helmet-async)

**설치:**
```bash
npm install react-helmet-async
```

**구현 구조:**
```
src/
├── components/
│   └── SEO/
│       ├── SEOHead.tsx        # 공통 SEO 컴포넌트
│       ├── JobPostingSEO.tsx  # 채용공고용
│       └── RegionalSEO.tsx    # 지역별 페이지용
```

**SEOHead.tsx 예시:**
```tsx
import { Helmet } from 'react-helmet-async';

interface SEOProps {
  title: string;
  description: string;
  url: string;
  image?: string;
  type?: 'website' | 'article';
}

export const SEOHead = ({ title, description, url, image, type = 'website' }: SEOProps) => (
  <Helmet>
    <title>{title} | 학교일자리</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={url} />
    
    {/* Open Graph */}
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={url} />
    <meta property="og:type" content={type} />
    {image && <meta property="og:image" content={image} />}
    
    {/* Twitter */}
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
  </Helmet>
);
```

#### 2-2. 채용공고 구조화 데이터 (JobPosting Schema)

**구글이 인식하는 채용공고 형식:**
```tsx
// JobPostingSchema.tsx
export const generateJobPostingSchema = (job: JobData) => ({
  "@context": "https://schema.org",
  "@type": "JobPosting",
  "title": job.title,
  "description": job.description,
  "datePosted": job.postedDate,
  "validThrough": job.deadline,
  "employmentType": job.employmentType, // FULL_TIME, PART_TIME, CONTRACT
  "hiringOrganization": {
    "@type": "Organization",
    "name": job.schoolName,
    "sameAs": job.schoolUrl
  },
  "jobLocation": {
    "@type": "Place",
    "address": {
      "@type": "PostalAddress",
      "addressLocality": job.city,
      "addressRegion": job.region,
      "addressCountry": "KR"
    }
  }
});
```

---

### Phase 3: 콘텐츠 확장 (1개월 이내) 📝

#### 3-1. 지역별 랜딩 페이지 생성

**목표:** "서울 교육청 채용", "경기도 학교 일자리" 등 지역 키워드 공략

**추천 URL 구조:**
```
/jobs/seoul        → 서울시 채용정보
/jobs/gyeonggi     → 경기도 채용정보
/jobs/incheon      → 인천시 채용정보
...
```

**각 페이지 필수 요소:**
- 고유 H1 타이틀 (예: "서울시 교육청 채용공고")
- 해당 지역 키워드 포함 description
- 지역별 구조화 데이터
- 실제 해당 지역 공고 목록

#### 3-2. 동적 sitemap 자동 생성

**Vite + SSG 또는 Cloudflare Functions 활용:**
```ts
// functions/sitemap.xml.ts (Cloudflare Pages Functions)
export const onRequest: PagesFunction = async () => {
  const jobs = await fetchActiveJobs(); // 활성 공고 조회
  
  const urls = jobs.map(job => `
    <url>
      <loc>https://학교일자리.com/jobs/${job.id}</loc>
      <lastmod>${job.updatedAt}</lastmod>
      <changefreq>daily</changefreq>
      <priority>0.7</priority>
    </url>
  `).join('');
  
  return new Response(`<?xml version="1.0" encoding="UTF-8"?>
    <urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
      ${staticPages}
      ${urls}
    </urlset>
  `, { headers: { 'Content-Type': 'application/xml' } });
};
```

---

### Phase 4: 장기 전략 (3개월+) 🚀

#### 4-1. 콘텐츠 마케팅

| 콘텐츠 유형 | 키워드 타겟 | 예시 |
|-------------|-------------|------|
| 블로그/가이드 | 정보성 키워드 | "교육공무직 면접 준비 가이드" |
| 지역 리포트 | 지역 키워드 | "2026년 서울시 교육청 채용 동향" |
| FAQ | 질문 키워드 | "학교 계약직 급여는 얼마인가요?" |

#### 4-2. 백링크 확보 전략

```
우선순위 높음:
├── 교육청 공식 사이트 링크 요청
├── 학교 홈페이지 "유용한 사이트" 등록
└── 교육 관련 커뮤니티 소개

우선순위 중간:
├── 취업 포털 사이트 제휴
├── 교육 관련 블로그 협업
└── 보도자료 배포
```

#### 4-3. 성과 측정 KPI

| 지표 | 측정 도구 | 목표 (6개월) |
|------|-----------|--------------|
| 검색 노출수 | Google Search Console | 10,000+/월 |
| 검색 클릭수 | Google Search Console | 500+/월 |
| 평균 순위 | Google Search Console | Top 20 |
| 색인 페이지 수 | site:학교일자리.com | 100+ |

---

## 🛠️ 우선순위 액션 아이템

### 🔴 긴급 (이번 주)
1. [ ] 구글 서치 콘솔 등록 상태 확인
2. [ ] 네이버 서치어드바이저 등록 상태 확인
3. [ ] sitemap.xml `lastmod` 날짜 업데이트
4. [ ] `site:학교일자리.com` 검색해서 현재 색인 상태 확인

### 🟡 중요 (2주 내)
5. [ ] react-helmet-async 설치 및 SEO 컴포넌트 구현
6. [ ] 채용공고 상세 페이지에 JobPosting 스키마 추가
7. [ ] 개별 공고 URL이 고유한지 확인 (/jobs/[id])

### 🟢 계획 (1개월 내)
8. [ ] 지역별 랜딩 페이지 최소 5개 생성
9. [ ] 동적 sitemap 구현
10. [ ] 첫 블로그/가이드 글 작성

---

## 📋 기술 구현 체크리스트

### 메타 태그 동적 변경
- [ ] App.tsx에 `HelmetProvider` 래핑
- [ ] 주요 페이지에 SEOHead 컴포넌트 적용
  - [ ] 홈페이지
  - [ ] 채용공고 목록 (/jobs)
  - [ ] 채용공고 상세 (/jobs/:id)
  - [ ] 지도 페이지 (/map)

### 구조화 데이터 확장
- [ ] JobPosting 스키마 구현
- [ ] Organization 스키마 추가
- [ ] BreadcrumbList 스키마 추가

### SPA SEO 대응
- [ ] Prerendering 검토 (Vite SSG / prerender.io)
- [ ] 또는 Cloudflare Workers 기반 SSR 검토

---

## 💡 핵심 포인트

> **SPA(React) 사이트의 SEO 한계**
> 
> 현재 학교일자리.com은 React SPA입니다. 
> 구글은 JavaScript를 렌더링하지만, 네이버는 제한적입니다.
> 
> **해결책:**
> 1. `react-helmet-async`로 메타 태그 동적 변경
> 2. Prerendering 서비스 사용 (prerender.io, renderton)
> 3. 장기적으로 Next.js 등 SSR 도입 검토

---

## 📞 다음 단계

**지금 바로 할 수 있는 것:**
1. 구글에서 `site:학교일자리.com` 검색 → 현재 색인 상태 확인
2. 네이버에서 `site:학교일자리.com` 검색 → 현재 색인 상태 확인
3. 서치 콘솔/서치어드바이저 대시보드 확인

**저한테 요청할 수 있는 것:**
- react-helmet-async 설치 및 SEO 컴포넌트 구현
- JobPosting 구조화 데이터 추가
- 지역별 랜딩 페이지 생성
- 동적 sitemap 구현

---

*이 플랜은 260201_검색상위노출구현플랜.md를 기반으로 현재 상태를 분석하여 업데이트한 것입니다.*
