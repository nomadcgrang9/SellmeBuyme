# Geocache 구현 플랜 - 카카오맵 API 한도 초과 문제 해결

**작성일**: 2026-02-02
**최종 수정**: 2026-02-02 23:30
**상태**: ✅ 구현 완료 (배포됨)
**긴급도**: 해결됨

---

## 1. 문제 상황

### 현황
| 지표 | 값 |
|------|-----|
| 총 공고 수 | 7,192개 |
| 고유 학교명 | 3,587개 |
| 좌표 있는 공고 | 2개 (수동 입력) |
| 좌표 없는 공고 | 7,190개 |

### 문제 원인
```
사용자 접속 → 학교명으로 Kakao Places API 호출 → 좌표 획득 → 마커 표시
                          ↓
            200명 × 300개 공고 = 60,000+ API 호출/일
                          ↓
            일일 쿼터 100,000 초과 → API 차단 → 마커 미표시
```

### API 사용량 (2월 1일)
- 성공: 132,796회
- 실패: 1,102,585회 (할당량 초과)
- 에러율: 89%

---

## 2. 해결 방안: Geocache 테이블

### 개념
```
Before: 사용자마다 카카오에 "학교 어디야?" 질문 (중복)
After:  한 번 물어본 학교는 DB에 저장 → 다음부터 DB에서 조회
```

### 기대 효과
| 항목 | Before | After |
|------|--------|-------|
| 학교당 API 호출 | 사용자 수만큼 | **1회 (최초 1회만)** |
| 일일 API 호출량 | 100,000+ | **최대 3,587회** |
| 재방문자 API 호출 | 매번 호출 | **0회** |

---

## 3. 구현 계획

### Phase 1: 테이블 생성 (10분)

**geocache 테이블 스키마**
```sql
CREATE TABLE geocache (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  organization VARCHAR(255) NOT NULL UNIQUE,  -- 학교/기관명
  latitude DECIMAL(10, 8) NOT NULL,
  longitude DECIMAL(11, 8) NOT NULL,
  source VARCHAR(50) DEFAULT 'kakao',  -- 출처 (kakao, neis, manual)
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 인덱스
CREATE INDEX idx_geocache_organization ON geocache(organization);

-- RLS 정책
ALTER TABLE geocache ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Anyone can read geocache" ON geocache FOR SELECT USING (true);
CREATE POLICY "Authenticated can insert geocache" ON geocache FOR INSERT WITH CHECK (true);
```

### Phase 2: API 함수 생성 (20분)

**src/lib/supabase/geocache.ts**
```typescript
// 1. 캐시 조회
export async function getGeocache(organization: string): Promise<{lat, lng} | null>

// 2. 캐시 저장
export async function saveGeocache(organization: string, lat: number, lng: number): Promise<void>

// 3. 여러 학교 일괄 조회
export async function getGeocacheBatch(organizations: string[]): Promise<Map<string, {lat, lng}>>
```

### Phase 3: Hero.tsx 수정 (30분)

**마커 생성 로직 변경**
```
현재 흐름:
1. localStorage 캐시 확인
2. 없으면 → Kakao API 호출
3. localStorage에 저장

변경 후:
1. localStorage 캐시 확인 (빠른 응답용)
2. 없으면 → Supabase geocache 조회 (서버 캐시)
3. 없으면 → Kakao API 호출
4. 결과를 geocache + localStorage 모두 저장
```

### Phase 4: 기존 데이터 마이그레이션 (선택)

**배치 스크립트로 기존 학교 좌표 채우기**
- 3,587개 학교를 배치로 geocoding
- 하루 1,000개씩 처리 (API 한도 고려)
- 또는 NEIS API 활용 (무료)

---

## 4. 파일 변경 목록

| 파일 | 작업 |
|------|------|
| `supabase/migrations/260202_create_geocache.sql` | 신규 생성 |
| `src/lib/supabase/geocache.ts` | 신규 생성 |
| `src/pages/new-landing/components/Hero.tsx` | 수정 |
| `src/types/index.ts` | 타입 추가 (선택) |

---

## 5. 구현 순서

```
[1] 마이그레이션 파일 작성 및 적용 (10분)
    ↓
[2] geocache.ts API 함수 작성 (20분)
    ↓
[3] Hero.tsx 마커 생성 로직 수정 (30분)
    ↓
[4] 로컬 테스트 (10분)
    ↓
[5] 빌드 & 배포 (5분)
    ↓
[6] 점검 모드 해제 (MaintenancePage.tsx)
    ↓
[7] 프로덕션 모니터링
```

**예상 소요 시간: 약 1.5시간**

---

## 6. 테스트 계획

### 로컬 테스트
- [ ] geocache 테이블 생성 확인
- [ ] geocache 조회/저장 함수 동작 확인
- [ ] Hero.tsx에서 geocache 우선 조회 확인
- [ ] Kakao API 호출 시 geocache 저장 확인
- [ ] 콘솔에서 캐시 히트/미스 로그 확인

### 프로덕션 테스트
- [ ] 마커 정상 표시 확인
- [ ] API 호출량 감소 확인 (카카오 콘솔)
- [ ] geocache 테이블 데이터 증가 확인

---

## 7. 롤백 계획

문제 발생 시:
1. `MAINTENANCE_MODE = true`로 점검 모드 재활성화
2. Hero.tsx에서 geocache 로직 주석 처리
3. 기존 localStorage 캐시만 사용하도록 복구

---

## 8. 후속 작업 (이번 주)

1. **크롤러 수정**: 공고 저장 시 geocache 확인 → 없으면 geocoding 후 저장
2. **배치 스크립트**: 기존 3,587개 학교 일괄 geocoding
3. **NEIS API 연동**: 무료 학교 좌표 데이터 활용 검토

---

## 9. 체크리스트

- [x] Phase 1: geocache 테이블 생성
- [x] Phase 2: geocache.ts API 함수 작성
- [x] Phase 3: Hero.tsx 수정
- [x] Phase 4: 로컬 테스트
- [x] Phase 5: 빌드 & 배포
- [x] Phase 6: 점검 모드 해제
- [x] Phase 7: 모니터링

---

## 10. 구현 결과 (2026-02-02)

### 10.1 최종 매칭률

| 카테고리 | Before | After |
|---------|--------|-------|
| **전체** | ~44% | **99.5%** (1101/1107) |
| **유치원** | 44% (54/123) | **100%** (123/123) |
| **특수학교** | ~25% | **100%** (74/74) |
| **일반학교** | ~92% | **99.3%** (914/920) |

### 10.2 geocache source별 통계

| source | 설명 | 개수 |
|--------|------|------|
| neis | 교육부 학교 데이터 | ~18,000 |
| kakao | Kakao Places API | ~2,000 |
| naver | Naver Local Search (fallback) | ~500 |
| manual | 수동 입력 | ~200 |
| region_center | 지역 중심 좌표 (fallback) | ~100 |
| school_mapping | 병설유치원→부모학교 | ~50 |
| abbrev | 축약명 매핑 | ~30 |

---

## 11. 발생한 추가 문제 및 해결

### 11.1 문제: 유치원/특수학교 마커 미표시

**증상**
- 유치원: 143개 중 23개만 표시
- 특수학교: 6개 중 0개 표시

**원인 분석**
```
job_postings.organization = "광주새롬학교"
geocache.organization = NULL (미등록)

→ LEFT JOIN 시 매칭 실패 → 좌표 없음 → 마커 미표시
```

**해결**
1. Supabase MCP로 미매칭 organization 목록 조회
2. 누락된 68개 유치원 + 83개 특수학교 geocache 수동 등록
3. 지역 중심 좌표(region_center) 활용하여 일괄 INSERT

### 11.2 문제: region_display_name NULL

**증상**
- 어제 900개 공고 → 오늘 684개만 표시
- DB에는 1,395개 활성 공고 존재

**원인**
```sql
-- 검색 쿼리가 region_display_name 기준으로 필터링
SELECT * FROM job_postings jp
JOIN crawl_boards cb ON jp.crawl_board_id = cb.id
WHERE cb.region_display_name = '경기'  -- NULL이면 제외됨!
```

**해결**
1. crawl_boards 테이블의 region_display_name NULL인 행 업데이트
2. approved_at도 함께 설정 (검색에 포함되려면 필요)
3. 크롤러에 자동 region_display_name 추출 로직 추가

---

## 12. 구현된 예방 시스템

### 12.1 다단계 Geocoding Fallback Chain

```
[crawler/lib/supabase.js - getOrCreateGeocode()]

1. geocache 조회 (캐시 히트)
   ↓ 실패
2. 병설유치원 → 부모학교 매핑
   ↓ 실패
3. Kakao Places API
   ↓ 실패 또는 쿼터 초과
4. Naver Local Search API (fallback)
   ↓ 실패
5. 축약명 재시도 ("전북특별자치도정읍교육지원청" → "정읍교육지원청")
   ↓ 실패
6. 로그 기록 후 NULL 반환 → Frontend에서 지역 중심 fallback 처리
```

### 12.2 Frontend 지역 중심 Fallback

```typescript
// [Hero.tsx]
const REGION_CENTER_COORDS = {
  '서울': { lat: 37.5665, lng: 126.9780 },
  '경기': { lat: 37.4138, lng: 127.5183 },
  // ... 17개 지역
};

// geocoding 실패 시 location에서 지역 추출 → 지역 중심에 랜덤 오프셋으로 마커 표시
```

### 12.3 자동 region_display_name 설정

```javascript
// [crawler/lib/supabase.js - extractRegionFromBoardName()]

// 신규 crawl_board 생성 시 이름에서 지역 자동 추출
"성남교육지원청" → region_display_name: "경기"
"부산광역시교육청" → region_display_name: "부산"

// 기존 board에 region_display_name 없으면 크롤링 시 자동 설정
```

---

## 13. 커밋 이력

| 커밋 | 내용 |
|------|------|
| `085c51d` | feat: geocache 구현으로 카카오맵 API 호출량 대폭 감소 |
| `ae561e1` | feat: 점검 모드 추가 및 API 호출 최적화 |
| `bc12271` | fix: 점검 모드 재활성화 - 마커 문제 해결 중 |
| `8e4335e` | fix: geocache 99.5% 매칭률 달성 및 점검 모드 해제 |

---

## 14. 향후 개선 과제

### 14.1 단기 (이번 주)

- [ ] **Naver API 키 설정**: 현재 미설정 상태, .env에 추가 필요
  ```
  NAVER_CLIENT_ID=xxx
  NAVER_CLIENT_SECRET=xxx
  ```

- [ ] **미매칭 6개 organization 처리**
  - 테스트 데이터: "글자수 테스트입니다..."
  - 불완전 항목: "초등학교", "학력", "미상", "행정실"
  - → 삭제 또는 무시 처리

### 14.2 중기 (2주 내)

- [ ] **AI 기반 Organization 정규화**
  - 크롤링 시 Gemini로 organization 이름 정규화
  - "서울OO초등학교" vs "OO초등학교" 통일
  - "OO초병설유치원" 자동 인식 및 부모학교 매핑

- [ ] **NEIS 데이터 정기 동기화**
  - 교육부 학교 기본정보 API 연동
  - 신설학교 자동 geocache 등록
  - 폐교 데이터 정리

- [ ] **geocache 품질 모니터링 대시보드**
  - source별 통계 시각화
  - 미매칭 organization 실시간 알림
  - 일일 API 호출량 추적

### 14.3 장기 (1개월 내)

- [ ] **좌표 정확도 개선**
  - region_center 좌표를 실제 주소 기반으로 교체
  - 카카오 주소 검색 API 활용
  - 학교 주소 데이터 확보 시 정확한 좌표로 업데이트

- [ ] **크롤러 geocoding 자동화**
  - 공고 저장 시 organization이 geocache에 없으면 자동 geocoding
  - 배치 작업 대신 실시간 처리
  - API 쿼터 모니터링 및 자동 throttling

---

## 15. 트러블슈팅 가이드

### 마커가 표시되지 않을 때

1. **브라우저 콘솔 확인**
   ```
   🔍 [Hero] geocache에서 조회: OO학교
   ✅ [Hero] geocache 히트: OO학교
   ❌ [Hero] geocache 미스: XX학교
   🔄 [Hero] 지역 중심 fallback: XX학교 → 경기
   ```

2. **DB 확인** (Supabase MCP)
   ```sql
   SELECT * FROM geocache WHERE organization = 'XX학교';
   ```

3. **수동 등록**
   ```sql
   INSERT INTO geocache (organization, latitude, longitude, source)
   VALUES ('XX학교', 37.5665, 126.9780, 'manual');
   ```

### Kakao API 쿼터 초과 시

1. 점검 모드 활성화
   ```typescript
   // src/components/MaintenancePage.tsx
   export const MAINTENANCE_MODE = true;
   ```

2. Naver API fallback 확인
   - .env에 NAVER_CLIENT_ID, NAVER_CLIENT_SECRET 설정 확인

3. geocache 커버리지 확인
   ```sql
   SELECT
     COUNT(DISTINCT jp.organization) as total,
     COUNT(DISTINCT CASE WHEN gc.latitude IS NOT NULL THEN jp.organization END) as matched
   FROM job_postings jp
   LEFT JOIN geocache gc ON jp.organization = gc.organization
   WHERE jp.deadline >= CURRENT_DATE;
   ```

---

## 16. 참고 자료

- Kakao Maps API: https://apis.map.kakao.com/
- Naver Local Search API: https://developers.naver.com/docs/serviceapi/search/local/local.md
- NEIS 학교 기본정보 API: https://open.neis.go.kr/portal/data/service/selectServicePage.do
- Supabase Geocache 테이블: `geocache` (21,000+ rows)
