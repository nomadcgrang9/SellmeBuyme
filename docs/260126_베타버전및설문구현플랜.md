# 베타 버전 표시 및 설문조사 구현 계획 (Google Forms 연동)

**작성일**: 2026-01-26
**목표**: 일주일간 베타 서비스 운영 및 사용자 피드백 수집
**구현 방식**: 베타 배지 + Google Forms 링크 모달

---

## 1. 베타 버전 표시 (옵션 A)

### 1.1 로고 옆 BETA 배지

```
┌─────────────────────────────┐
│  [학교일자리 로고] [BETA]   │  ← 로고 우측에 작은 배지
│                             │
│  [필터] [검색]              │
└─────────────────────────────┘
```

**구현 위치**:
- `src/pages/new-landing/components/Hero.tsx` - 왼쪽 패널 로고 옆

**환경 변수 제어**:
```env
# .env
VITE_BETA_END_DATE=2026-02-02
VITE_IS_BETA=true
```

---

## 2. 설문조사 모달 타이밍 전략

### 2.1 실제 베타 서비스 타이밍 시나리오

#### 🏆 시나리오 1: Exit Intent (떠나려 할 때) - **최고 응답률**
**타이밍**: 사용자가 페이지를 벗어나려고 할 때
- 마우스가 브라우저 상단(탭/주소창)으로 빠르게 이동
- 뒤로가기 버튼 클릭
- 다른 탭으로 전환

**장점**:
- 응답률 높음 (이미 경험을 마쳤으므로 의견 형성됨)
- 서비스 사용 중 방해하지 않음
- "마지막 기회" 심리 활용

**실제 사례**: Hotjar, Typeform, Notion 등이 사용

**단점**:
- 모바일에서 구현 어려움 (마우스 없음)
- 빠르게 떠나는 사용자는 무시 가능

---

#### 🎯 시나리오 2: 일정 시간 + 행동 조합 (추천)
**타이밍**: 사용자가 서비스를 충분히 경험한 후
- 첫 방문 후 **3분 경과** + 공고 **최소 3개 조회**
- 또는 **5분 이상 체류**
- 또는 **지도 이동 + 필터 사용**

**장점**:
- 진지한 사용자만 타겟팅 (응답 품질 높음)
- 충분한 경험 후 피드백 (구체적)
- 데스크톱/모바일 모두 가능

**실제 사례**: Figma, Linear, Slack 베타

**구현 예시**:
```javascript
// 조건: 3분 + 공고 3개 클릭
if (elapsedTime > 3min && jobClickCount >= 3) {
  showSurveyModal()
}
```

---

#### ⏰ 시나리오 3: 재방문 시점
**타이밍**: 2번째 또는 3번째 방문 시
- 첫 방문: 서비스 탐색 (설문 안 띄움)
- 2번째 방문: 재방문 의사 있는 사용자 → 설문 표시

**장점**:
- 재방문 = 관심 있는 사용자
- 충분한 경험 보유
- 응답 품질 매우 높음

**실제 사례**: Loom, Miro, Airtable

**구현 예시**:
```javascript
const visitCount = parseInt(localStorage.getItem('visit_count') || '0')
if (visitCount === 2) {
  showSurveyModal()
}
```

---

#### 💡 시나리오 4: 핵심 기능 사용 후
**타이밍**: 특정 기능을 처음 사용한 직후
- **길찾기** 사용 직후 (이 기능이 핵심이라면)
- **마커 등록** 시도 후
- **로그인** 완료 후

**장점**:
- 특정 기능에 대한 피드백 수집
- 사용자가 집중된 상태
- 맥락이 명확함

**실제 사례**: GitHub Copilot, Vercel, Railway

**구현 예시**:
```javascript
// 길찾기 완료 후
onDirectionsComplete() {
  if (!hasSeenSurvey) {
    showSurveyModal()
  }
}
```

---

#### 🕒 시나리오 5: 비활성 상태 감지
**타이밍**: 사용자가 30초~1분간 아무 행동 안 할 때
- 공고 상세 페이지에서 읽는 중
- 지도를 보고 고민 중

**장점**:
- 사용 중 방해하지 않음
- 여유 있는 타이밍
- 자연스러운 개입

**실제 사례**: Medium, Substack, Dev.to

**단점**:
- 정말 idle인지 구분 어려움
- 너무 빈번하면 짜증

---

#### 📊 시나리오 6: 스크롤 깊이 기반 (웹사이트 적합)
**타이밍**: 페이지 스크롤 75% 도달 시
- 공고 목록을 절반 이상 본 경우

**장점**:
- 충분한 콘텐츠 소비
- 관심도 높은 사용자

**실제 사례**: 콘텐츠 사이트, 블로그

**단점**:
- 지도 기반 UI에는 부적합 (스크롤 없음)

---

### 2.2 **추천 조합 전략**

#### 전략 A: 다단계 트리거 (가장 효과적)
```
1차 기회: 3분 + 공고 3개 클릭 → 작은 플로팅 버튼만 표시
2차 기회: 5분 경과 → 모달 자동 팝업 (한 번만)
3차 기회: Exit Intent → 마지막 모달 (데스크톱만)
```

**응답률 예상**: 15~25%

---

#### 전략 B: 재방문 중심 (응답 품질 중시)
```
1차 방문: 설문 없음 (서비스 탐색만)
2차 방문: 입장 1분 후 모달 팝업
3차+ 방문: 플로팅 버튼만 유지
```

**응답률 예상**: 5~10% (하지만 매우 고품질)

---

#### 전략 C: 핵심 기능 중심
```
조건: 길찾기 사용 OR 마커 등록 시도 OR 로그인 완료
→ 즉시 감사 메시지 + 설문 모달
"기능을 사용해주셔서 감사합니다! 1분만 시간 내주시겠어요?"
```

**응답률 예상**: 20~30% (맥락이 명확해서 높음)

---

### 2.3 최종 추천: **하이브리드 전략**

```javascript
// 의사코드
function decideSurveyTiming() {
  const visitCount = getVisitCount()
  const elapsedTime = getElapsedTime()
  const jobClickCount = getJobClickCount()
  const usedDirections = hasUsedDirections()

  // 이미 응답했으면 무조건 skip
  if (hasCompletedSurvey()) return 'skip'

  // 우선순위 1: 핵심 기능 사용 직후 (가장 효과적)
  if (usedDirections && !hasSeenModal()) {
    return 'show_modal_immediately'
  }

  // 우선순위 2: 재방문자 (고품질 응답)
  if (visitCount === 2 && elapsedTime > 60000) { // 2번째 방문, 1분 후
    return 'show_modal'
  }

  // 우선순위 3: 충분한 경험 후 (일반 사용자)
  if (elapsedTime > 180000 && jobClickCount >= 3) { // 3분 + 공고 3개
    return 'show_floating_button'
  }

  // 우선순위 4: Exit Intent (마지막 기회, 데스크톱만)
  if (isExitIntent() && !isMobile()) {
    return 'show_modal_exit'
  }

  // 아직 조건 안 됨
  return 'wait'
}
```

---

## 3. 모달 UX 설계

### 3.1 모달 디자인 (Anti-Vibe 미니멀)

```
┌────────────────────────────────────────┐
│                                        │
│        💬                              │
│                                        │
│   베타 서비스 개선을 위해              │
│   의견을 들려주세요                    │
│                                        │
│   소요 시간: 1분                       │
│   감사 선물: 추첨 스타벅스 기프티콘    │
│                                        │
│   [설문 참여하기]  [나중에]            │
│                                        │
│   [ ] 다시 보지 않기                   │
└────────────────────────────────────────┘
```

**모달 특성**:
- 중앙 배치, 반투명 배경 (backdrop-blur)
- 애니메이션: fade-in + scale (0.95 → 1.0)
- ESC 키로 닫기 가능
- 배경 클릭 시 "나중에" 처리

---

### 3.2 플로팅 버튼 디자인 (덜 공격적)

```
우측 하단:
┌─────────────┐
│   💬 설문   │  ← 펄스 애니메이션
└─────────────┘
```

**위치**: 로그인 버튼 바로 위 (우측 하단)
- 데스크톱: `bottom-20 right-4`
- 모바일: 화면 작아서 부담스러우면 숨김 가능

**클릭 시**: Google Forms 새 탭 열기

---

### 3.3 감사 메시지 (응답 완료 후)

사용자가 설문을 완료하고 돌아왔을 때:
```
┌────────────────────────────────────────┐
│        ✨                              │
│                                        │
│   소중한 의견 감사합니다!              │
│                                        │
│   더 나은 서비스로 찾아뵙겠습니다.     │
│                                        │
│   [확인]                               │
└────────────────────────────────────────┘
```

**표시 시점**:
- 설문 링크 클릭 후 창 닫고 돌아왔을 때
- `window.focus` 이벤트로 감지
- 자동으로 5초 후 사라짐

---

## 4. Google Forms 연동 방식

### 4.1 링크 열기 방식

#### 옵션 A: 새 탭 (권장)
```javascript
window.open('https://forms.gle/YOUR_FORM_ID', '_blank')
```

**장점**:
- 사용자가 서비스와 설문을 동시에 볼 수 있음
- 설문 도중 서비스 참고 가능

**단점**:
- 팝업 차단 가능성 (드물긴 함)

#### 옵션 B: 같은 탭
```javascript
window.location.href = 'https://forms.gle/YOUR_FORM_ID'
```

**단점**: 서비스 이탈, 뒤로가기 필요

#### 옵션 C: iframe 모달 (비권장)
- Google Forms의 크로스 도메인 제한으로 완전한 제어 어려움
- 모바일에서 스크롤 이슈

---

### 4.2 응답 추적

**문제**: Google Forms는 외부 JS에서 제출 여부 확인 불가

**해결책**:
```javascript
// 링크 클릭 시
function openSurvey() {
  const surveyWindow = window.open('https://forms.gle/...', '_blank')
  localStorage.setItem('survey_link_clicked', Date.now())

  // 창 포커스 돌아왔을 때 (사용자가 다시 돌아옴)
  window.addEventListener('focus', function checkReturn() {
    const clickedTime = localStorage.getItem('survey_link_clicked')
    if (clickedTime && Date.now() - clickedTime > 30000) { // 30초 이상 경과
      // 설문 완료로 간주
      localStorage.setItem('survey_completed', 'true')
      showThankYouMessage()
      window.removeEventListener('focus', checkReturn)
    }
  }, { once: true })
}
```

**가정**: 30초 이상 설문 탭에 있었으면 응답했을 가능성 높음

---

## 5. 로컬스토리지 관리

### 5.1 저장 항목

```javascript
{
  // 방문 추적
  "visit_count": "2",
  "first_visit_time": "1737878400000",
  "last_visit_time": "1737964800000",

  // 행동 추적
  "job_click_count": "5",
  "used_directions": "true",
  "used_filters": "true",

  // 설문 상태
  "survey_link_clicked": "1737878400000",
  "survey_completed": "true",
  "survey_dismissed_count": "2", // "나중에" 누른 횟수
  "never_show_survey": "false" // "다시 보지 않기" 체크
}
```

---

## 6. 구현 단계

### Phase 1: 베타 배지 구현 (30분)

**파일**:
- `src/components/common/BetaBadge.tsx` (신규)
- `src/pages/new-landing/components/Hero.tsx` (수정)

```tsx
// src/components/common/BetaBadge.tsx
export function BetaBadge() {
  const isBeta = import.meta.env.VITE_IS_BETA === 'true'
  const endDate = import.meta.env.VITE_BETA_END_DATE

  if (!isBeta || (endDate && new Date() > new Date(endDate))) {
    return null
  }

  return (
    <span className="ml-2 px-2 py-0.5 text-[10px] font-bold bg-gradient-to-r from-orange-500 to-red-500 text-white rounded shadow-sm animate-pulse">
      BETA
    </span>
  )
}
```

**Hero.tsx 수정**:
```tsx
import { BetaBadge } from '@/components/common/BetaBadge'

// 로고 부분
<div className="px-3 py-3 border-b border-gray-200 flex-shrink-0">
  <a href="/" className="flex items-center justify-center">
    <img src="/logo.png" alt="쌤찾기" className="h-[68px] w-auto" />
    <BetaBadge />
  </a>
</div>
```

---

### Phase 2: 설문 훅 구현 (1시간)

**파일**:
- `src/hooks/useSurveyTrigger.ts` (신규)
- `src/lib/utils/surveyTracking.ts` (신규)

```tsx
// src/lib/utils/surveyTracking.ts
export const SurveyTracker = {
  // 방문 카운트 증가
  incrementVisit(): number {
    const count = parseInt(localStorage.getItem('visit_count') || '0') + 1
    localStorage.setItem('visit_count', count.toString())

    if (count === 1) {
      localStorage.setItem('first_visit_time', Date.now().toString())
    }
    localStorage.setItem('last_visit_time', Date.now().toString())

    return count
  },

  // 공고 클릭 카운트 증가
  incrementJobClick(): number {
    const count = parseInt(localStorage.getItem('job_click_count') || '0') + 1
    localStorage.setItem('job_click_count', count.toString())
    return count
  },

  // 길찾기 사용 기록
  markDirectionsUsed(): void {
    localStorage.setItem('used_directions', 'true')
  },

  // 설문 완료 여부
  hasSurveyCompleted(): boolean {
    return localStorage.getItem('survey_completed') === 'true'
  },

  // 다시 보지 않기
  hasNeverShowSurvey(): boolean {
    return localStorage.getItem('never_show_survey') === 'true'
  },

  // 설문 모달 표시 횟수
  getSurveyShownCount(): number {
    return parseInt(localStorage.getItem('survey_shown_count') || '0')
  },

  incrementSurveyShown(): void {
    const count = this.getSurveyShownCount() + 1
    localStorage.setItem('survey_shown_count', count.toString())
  },

  // 설문 완료 처리
  markSurveyCompleted(): void {
    localStorage.setItem('survey_completed', 'true')
  },

  // 다시 보지 않기 처리
  markNeverShow(): void {
    localStorage.setItem('never_show_survey', 'true')
  }
}
```

```tsx
// src/hooks/useSurveyTrigger.ts
import { useState, useEffect, useCallback } from 'react'
import { SurveyTracker } from '@/lib/utils/surveyTracking'

export type SurveyTriggerType =
  | 'modal_immediately' // 즉시 모달
  | 'floating_button'   // 플로팅 버튼만
  | 'exit_intent'       // Exit intent 모달
  | 'skip'              // 표시 안 함

export function useSurveyTrigger() {
  const [triggerType, setTriggerType] = useState<SurveyTriggerType>('skip')
  const [startTime] = useState(Date.now())

  useEffect(() => {
    // 초기 방문 카운트 증가
    SurveyTracker.incrementVisit()
  }, [])

  const checkTrigger = useCallback(() => {
    // 이미 완료했거나 다시 보지 않기 선택
    if (SurveyTracker.hasSurveyCompleted() || SurveyTracker.hasNeverShowSurvey()) {
      return 'skip'
    }

    const visitCount = parseInt(localStorage.getItem('visit_count') || '0')
    const elapsedTime = Date.now() - startTime
    const jobClickCount = parseInt(localStorage.getItem('job_click_count') || '0')
    const usedDirections = localStorage.getItem('used_directions') === 'true'
    const shownCount = SurveyTracker.getSurveyShownCount()

    // 너무 많이 보여줬으면 중단 (최대 3번)
    if (shownCount >= 3) {
      return 'skip'
    }

    // 우선순위 1: 길찾기 사용 직후 (가장 효과적)
    if (usedDirections && shownCount === 0) {
      return 'modal_immediately'
    }

    // 우선순위 2: 2번째 방문 + 1분 경과
    if (visitCount === 2 && elapsedTime > 60000) {
      return 'modal_immediately'
    }

    // 우선순위 3: 3분 경과 + 공고 3개 클릭
    if (elapsedTime > 180000 && jobClickCount >= 3) {
      return 'floating_button'
    }

    return 'skip'
  }, [startTime])

  useEffect(() => {
    // 1초마다 트리거 조건 체크
    const interval = setInterval(() => {
      const newTrigger = checkTrigger()
      if (newTrigger !== 'skip') {
        setTriggerType(newTrigger)
        clearInterval(interval)
      }
    }, 1000)

    return () => clearInterval(interval)
  }, [checkTrigger])

  return {
    triggerType,
    dismiss: () => setTriggerType('skip'),
  }
}
```

---

### Phase 3: 설문 모달/버튼 UI 구현 (1.5시간)

**파일**:
- `src/components/survey/SurveyModal.tsx` (신규)
- `src/components/survey/SurveyFloatingButton.tsx` (신규)
- `src/components/survey/ThankYouModal.tsx` (신규)

```tsx
// src/components/survey/SurveyModal.tsx
import { useState } from 'react'
import { SurveyTracker } from '@/lib/utils/surveyTracking'

interface Props {
  isOpen: boolean
  onClose: () => void
}

export function SurveyModal({ isOpen, onClose }: Props) {
  const [neverShow, setNeverShow] = useState(false)

  if (!isOpen) return null

  const handleParticipate = () => {
    const surveyUrl = import.meta.env.VITE_SURVEY_URL || 'https://forms.gle/YOUR_FORM_ID'

    // 새 탭에서 설문 열기
    window.open(surveyUrl, '_blank')

    // 클릭 시간 기록
    localStorage.setItem('survey_link_clicked', Date.now().toString())

    // 30초 후 돌아오면 완료로 간주
    const focusHandler = () => {
      const clickedTime = localStorage.getItem('survey_link_clicked')
      if (clickedTime && Date.now() - parseInt(clickedTime) > 30000) {
        SurveyTracker.markSurveyCompleted()
        // ThankYouModal 표시는 부모에서 처리
      }
      window.removeEventListener('focus', focusHandler)
    }
    window.addEventListener('focus', focusHandler)

    onClose()
  }

  const handleDismiss = () => {
    if (neverShow) {
      SurveyTracker.markNeverShow()
    }
    SurveyTracker.incrementSurveyShown()
    onClose()
  }

  return (
    <div
      className="fixed inset-0 z-50 flex items-center justify-center bg-black/30 backdrop-blur-sm animate-fadeIn"
      onClick={handleDismiss}
    >
      <div
        className="bg-white rounded-2xl p-8 max-w-sm mx-4 text-center shadow-2xl animate-scaleIn"
        style={{
          animation: 'scaleIn 0.3s cubic-bezier(0.34, 1.56, 0.64, 1)'
        }}
        onClick={(e) => e.stopPropagation()}
      >
        {/* 아이콘 */}
        <div className="text-5xl mb-4">💬</div>

        {/* 제목 */}
        <h3 className="text-xl font-bold text-gray-800 mb-2">
          베타 서비스 개선을 위해<br />
          의견을 들려주세요
        </h3>

        {/* 부제 */}
        <p className="text-sm text-gray-500 mb-1">
          소요 시간: 1분
        </p>
        <p className="text-xs text-orange-600 font-semibold mb-6">
          감사 선물: 추첨 스타벅스 기프티콘
        </p>

        {/* 버튼 */}
        <div className="flex flex-col gap-3">
          <button
            onClick={handleParticipate}
            className="w-full px-6 py-3 text-sm font-semibold text-white bg-gradient-to-r from-blue-600 to-blue-700 rounded-xl hover:from-blue-700 hover:to-blue-800 transition-all shadow-md hover:shadow-lg"
          >
            설문 참여하기
          </button>
          <button
            onClick={handleDismiss}
            className="w-full px-6 py-2.5 text-sm font-medium text-gray-600 bg-gray-100 rounded-xl hover:bg-gray-200 transition-colors"
          >
            나중에
          </button>
        </div>

        {/* 다시 보지 않기 */}
        <label className="flex items-center justify-center gap-2 mt-4 cursor-pointer text-xs text-gray-500 hover:text-gray-700">
          <input
            type="checkbox"
            checked={neverShow}
            onChange={(e) => setNeverShow(e.target.checked)}
            className="w-3.5 h-3.5 rounded border-gray-300"
          />
          <span>다시 보지 않기</span>
        </label>
      </div>

      <style>{`
        @keyframes scaleIn {
          from {
            opacity: 0;
            transform: scale(0.95);
          }
          to {
            opacity: 1;
            transform: scale(1);
          }
        }
      `}</style>
    </div>
  )
}
```

```tsx
// src/components/survey/SurveyFloatingButton.tsx
interface Props {
  onClick: () => void
}

export function SurveyFloatingButton({ onClick }: Props) {
  return (
    <button
      onClick={onClick}
      className="fixed bottom-20 md:bottom-20 right-4 z-30 px-4 py-2.5 bg-gradient-to-r from-orange-500 to-red-500 text-white text-sm font-semibold rounded-full shadow-lg hover:shadow-xl hover:scale-105 transition-all animate-pulse"
      style={{
        boxShadow: '0 4px 20px rgba(249, 115, 22, 0.4)'
      }}
    >
      💬 설문조사
    </button>
  )
}
```

```tsx
// src/components/survey/ThankYouModal.tsx
interface Props {
  isOpen: boolean
  onClose: () => void
}

export function ThankYouModal({ isOpen, onClose }: Props) {
  if (!isOpen) return null

  // 5초 후 자동 닫기
  setTimeout(() => {
    onClose()
  }, 5000)

  return (
    <div
      className="fixed inset-0 z-50 flex items-center justify-center bg-black/20 backdrop-blur-sm animate-fadeIn"
      onClick={onClose}
    >
      <div
        className="bg-white rounded-2xl p-8 max-w-sm mx-4 text-center shadow-2xl animate-scaleIn"
        onClick={(e) => e.stopPropagation()}
      >
        {/* 아이콘 */}
        <div className="text-5xl mb-4">✨</div>

        {/* 제목 */}
        <h3 className="text-xl font-bold text-gray-800 mb-3">
          소중한 의견 감사합니다!
        </h3>

        {/* 내용 */}
        <p className="text-sm text-gray-600 mb-6">
          더 나은 서비스로 찾아뵙겠습니다.
        </p>

        {/* 버튼 */}
        <button
          onClick={onClose}
          className="px-6 py-2.5 text-sm font-semibold text-white bg-gray-800 rounded-xl hover:bg-gray-700 transition-colors"
        >
          확인
        </button>
      </div>
    </div>
  )
}
```

---

### Phase 4: Hero.tsx 통합 (30분)

```tsx
// src/pages/new-landing/components/Hero.tsx
import { useState, useEffect } from 'react'
import { SurveyModal } from '@/components/survey/SurveyModal'
import { SurveyFloatingButton } from '@/components/survey/SurveyFloatingButton'
import { ThankYouModal } from '@/components/survey/ThankYouModal'
import { useSurveyTrigger } from '@/hooks/useSurveyTrigger'
import { SurveyTracker } from '@/lib/utils/surveyTracking'

export const Hero: React.FC = () => {
  const { triggerType, dismiss } = useSurveyTrigger()
  const [isSurveyModalOpen, setIsSurveyModalOpen] = useState(false)
  const [isThankYouModalOpen, setIsThankYouModalOpen] = useState(false)

  // 트리거 타입에 따라 모달 표시
  useEffect(() => {
    if (triggerType === 'modal_immediately') {
      setIsSurveyModalOpen(true)
      SurveyTracker.incrementSurveyShown()
    }
  }, [triggerType])

  // 공고 클릭 시 추적
  const handleCardClick = useCallback((job: JobPostingCard) => {
    SurveyTracker.incrementJobClick()
    setSelectedJob(job)
    // ... 기존 로직
  }, [])

  // 길찾기 사용 추적
  const handleDirectionsClick = useCallback((job: JobPostingCard) => {
    SurveyTracker.markDirectionsUsed()
    // ... 기존 로직
  }, [])

  // 설문 완료 후 감사 모달 표시
  useEffect(() => {
    const checkSurveyComplete = () => {
      if (SurveyTracker.hasSurveyCompleted() && !isThankYouModalOpen) {
        setIsThankYouModalOpen(true)
      }
    }

    window.addEventListener('focus', checkSurveyComplete)
    return () => window.removeEventListener('focus', checkSurveyComplete)
  }, [isThankYouModalOpen])

  return (
    <section className="h-full w-full relative">
      {/* 기존 Hero 코드... */}

      {/* 설문 모달 */}
      <SurveyModal
        isOpen={isSurveyModalOpen}
        onClose={() => {
          setIsSurveyModalOpen(false)
          dismiss()
        }}
      />

      {/* 플로팅 버튼 */}
      {triggerType === 'floating_button' && (
        <SurveyFloatingButton
          onClick={() => {
            setIsSurveyModalOpen(true)
            SurveyTracker.incrementSurveyShown()
          }}
        />
      )}

      {/* 감사 모달 */}
      <ThankYouModal
        isOpen={isThankYouModalOpen}
        onClose={() => setIsThankYouModalOpen(false)}
      />
    </section>
  )
}
```

---

### Phase 5: Exit Intent 구현 (선택, 1시간)

```tsx
// src/hooks/useExitIntent.ts
import { useEffect, useState } from 'react'

export function useExitIntent() {
  const [shouldTrigger, setShouldTrigger] = useState(false)

  useEffect(() => {
    // 모바일은 지원 안 함
    if ('ontouchstart' in window) return

    let triggered = false

    const handleMouseLeave = (e: MouseEvent) => {
      // 마우스가 화면 상단으로 빠르게 이동 (탭 닫기 시도)
      if (e.clientY <= 0 && !triggered) {
        triggered = true
        setShouldTrigger(true)
      }
    }

    document.addEventListener('mouseleave', handleMouseLeave)
    return () => document.removeEventListener('mouseleave', handleMouseLeave)
  }, [])

  return shouldTrigger
}
```

**Hero.tsx에 통합**:
```tsx
const exitIntent = useExitIntent()

useEffect(() => {
  if (exitIntent && triggerType !== 'skip' && !SurveyTracker.hasSurveyCompleted()) {
    setIsSurveyModalOpen(true)
    SurveyTracker.incrementSurveyShown()
  }
}, [exitIntent, triggerType])
```

---

## 7. 환경 변수 설정

```env
# .env
VITE_IS_BETA=true
VITE_BETA_END_DATE=2026-02-02
VITE_SURVEY_URL=https://forms.gle/YOUR_ACTUAL_FORM_ID
```

---

## 8. 체크리스트

### 베타 배지
- [ ] `.env`에 `VITE_IS_BETA`, `VITE_BETA_END_DATE` 설정
- [ ] `BetaBadge.tsx` 컴포넌트 생성
- [ ] `Hero.tsx` 로고 옆에 배지 추가
- [ ] 모바일 반응형 확인

### 설문 시스템
- [ ] `surveyTracking.ts` 유틸 작성
- [ ] `useSurveyTrigger.ts` 훅 작성
- [ ] `SurveyModal.tsx` 컴포넌트 생성
- [ ] `SurveyFloatingButton.tsx` 컴포넌트 생성
- [ ] `ThankYouModal.tsx` 컴포넌트 생성
- [ ] `Hero.tsx`에 통합 (공고 클릭, 길찾기 추적 포함)
- [ ] Google Forms URL 환경 변수 설정
- [ ] 로컬스토리지 동작 테스트
- [ ] 모바일/데스크톱 UI 확인

### 선택사항
- [ ] `useExitIntent.ts` 훅 작성 (데스크톱만)
- [ ] Exit Intent 통합

---

## 9. 타임라인

| 작업 | 예상 시간 |
|------|----------|
| Phase 1: 베타 배지 | 30분 |
| Phase 2: 설문 훅 | 1시간 |
| Phase 3: UI 컴포넌트 | 1.5시간 |
| Phase 4: Hero 통합 | 30분 |
| Phase 5: Exit Intent (선택) | 1시간 |
| 테스트 & 디버깅 | 1시간 |
| **총 (필수만)** | **3.5시간** |
| **총 (Exit Intent 포함)** | **4.5시간** |

---

## 10. 테스트 시나리오

1. **첫 방문 사용자**
   - 3분 경과 + 공고 3개 클릭 → 플로팅 버튼 표시 확인
   - 플로팅 버튼 클릭 → 구글 폼 새 탭 열림
   - 돌아왔을 때 감사 모달 표시

2. **길찾기 사용자**
   - 길찾기 사용 직후 → 즉시 모달 팝업 확인

3. **재방문자**
   - 2번째 방문 1분 후 → 모달 팝업 확인

4. **"나중에" 선택**
   - 최대 3번까지만 표시되는지 확인

5. **"다시 보지 않기" 선택**
   - 이후 설문 안 뜨는지 확인

6. **Exit Intent (데스크톱)**
   - 마우스를 탭 쪽으로 이동 → 모달 표시

---

## 11. 주의사항

1. **팝업 차단**: `window.open()` 사용 시 일부 브라우저에서 차단 가능 (드물지만 안내 필요)
2. **중복 방지**: 로컬스토리지 기반이므로 시크릿 모드나 다른 브라우저에서는 재표시됨
3. **모바일 최적화**: 플로팅 버튼이 너무 많으면 UI 복잡 → 필요시 모바일에서 숨김
4. **베타 종료 후**: `VITE_IS_BETA=false`로 변경하고 재배포

---

## 12. 최종 추천 전략

**1주일 베타 기간 기준**:
- **Day 1-3**: 길찾기/마커 등록 사용 직후 즉시 모달 (핵심 기능 피드백)
- **Day 4-5**: 3분 + 공고 3개 조건 달성 시 플로팅 버튼 (일반 사용자)
- **Day 6-7**: 재방문자에게 적극적으로 모달 표시 (고품질 응답 확보)
- **전체 기간**: Exit Intent 활성화 (데스크톱, 마지막 기회)

**예상 응답 수** (방문자 1000명 기준):
- 핵심 기능 사용자: 200명 × 30% = 60건
- 일반 사용자: 500명 × 15% = 75건
- 재방문자: 100명 × 25% = 25건
- Exit Intent: 200명 × 10% = 20건
- **총 예상: 180건 (18% 응답률)**
```
우측 하단에 작은 플로팅 버튼:
┌─────────────────┐
│                 │
│     💬 설문     │  ← 클릭하면 모달 열림
│                 │
└─────────────────┘
```

**장점**:
- 사용자가 원할 때 참여
- 화면 방해 최소화
- 닫기/무시 가능

#### 옵션 B: 자동 팝업 모달 (적극적)
```
3분 사용 후 자동으로 중앙 모달:
┌────────────────────────────────┐
│  베타 서비스 만족도 조사      │
│  (소요 시간: 1분)              │
│                                │
│  [설문 참여하기] [나중에]     │
└────────────────────────────────┘
```

**주의**: 너무 빨리 표시하면 거부감

### 2.3 설문 문항 설계

```markdown
### 학교일자리 베타 서비스 설문조사 (1분 소요)

1. 서비스를 어떻게 알게 되셨나요? (단일 선택)
   - [ ] 검색 엔진 (구글, 네이버 등)
   - [ ] SNS (카카오톡, 인스타그램 등)
   - [ ] 지인 추천
   - [ ] 교육청/학교 공지
   - [ ] 기타: _________

2. 주로 찾으시는 정보는 무엇인가요? (복수 선택 가능)
   - [ ] 정규직 교사 채용
   - [ ] 기간제/시간강사 공고
   - [ ] 방과후 강사 공고
   - [ ] 특정 지역 공고
   - [ ] 특정 과목 공고

3. 서비스 이용 만족도를 평가해주세요. (1~5점)
   - 공고 검색의 편리함: ⭐⭐⭐⭐⭐
   - 지도 기능의 유용성: ⭐⭐⭐⭐⭐
   - 모바일 사용 편의성: ⭐⭐⭐⭐⭐
   - 전체 만족도: ⭐⭐⭐⭐⭐

4. 가장 유용했던 기능은 무엇인가요? (단일 선택)
   - [ ] 지도에서 공고 확인
   - [ ] 학교급별 필터
   - [ ] 지역 검색
   - [ ] 마감임박 공고 표시
   - [ ] 길찾기 기능

5. 개선이 필요한 점이나 추가되었으면 하는 기능이 있나요? (자유 서술)
   [                                    ]
   [                                    ]

6. 정식 오픈 시 다시 방문하실 의향이 있으신가요?
   - [ ] 매우 그렇다
   - [ ] 그렇다
   - [ ] 보통이다
   - [ ] 아니다

감사합니다! 소중한 의견으로 더 나은 서비스를 만들겠습니다.
```

### 2.4 데이터베이스 스키마

```sql
-- survey_responses 테이블
CREATE TABLE survey_responses (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES auth.users(id) ON DELETE SET NULL, -- 비로그인 사용자는 NULL
  session_id TEXT NOT NULL, -- 익명 사용자 추적용 (브라우저 지문)

  -- 응답 데이터 (JSONB로 유연하게 저장)
  responses JSONB NOT NULL,

  -- 메타데이터
  device_type TEXT, -- 'mobile' | 'desktop'
  browser TEXT,
  referrer TEXT,

  created_at TIMESTAMPTZ DEFAULT now(),

  -- 중복 방지: 같은 세션에서 1회만 응답
  UNIQUE(session_id)
);

-- 응답 예시 (responses JSONB)
{
  "discovery_channel": "검색 엔진",
  "interest_types": ["정규직 교사 채용", "기간제/시간강사 공고"],
  "ratings": {
    "search_convenience": 5,
    "map_usefulness": 4,
    "mobile_ux": 5,
    "overall": 5
  },
  "most_useful_feature": "지도에서 공고 확인",
  "improvement_suggestions": "알림 기능이 있었으면 좋겠어요",
  "revisit_intention": "매우 그렇다"
}

-- 인덱스
CREATE INDEX idx_survey_created_at ON survey_responses(created_at);
CREATE INDEX idx_survey_user_id ON survey_responses(user_id);
```

---

## 3. 구현 단계

### Phase 1: 베타 배지 표시 (30분)

1. **환경 변수 설정**
   ```env
   VITE_BETA_END_DATE=2026-02-02
   VITE_IS_BETA=true
   ```

2. **BetaBadge 컴포넌트 생성**
   ```tsx
   // src/components/common/BetaBadge.tsx
   export function BetaBadge() {
     const isBeta = import.meta.env.VITE_IS_BETA === 'true'
     const endDate = import.meta.env.VITE_BETA_END_DATE

     if (!isBeta || (endDate && new Date() > new Date(endDate))) {
       return null
     }

     return (
       <span className="px-2 py-0.5 text-xs font-bold bg-orange-500 text-white rounded">
         BETA
       </span>
     )
   }
   ```

3. **Hero.tsx에 배지 추가**
   - 로고 우측에 BetaBadge 컴포넌트 배치

### Phase 2: 설문 인프라 구축 (1시간)

1. **DB 마이그레이션**
   ```bash
   # supabase/migrations/20260126_add_survey_tables.sql
   ```

2. **세션 ID 생성 유틸**
   ```tsx
   // src/lib/utils/sessionId.ts
   export function getOrCreateSessionId(): string {
     let sessionId = localStorage.getItem('survey_session_id')
     if (!sessionId) {
       sessionId = crypto.randomUUID()
       localStorage.setItem('survey_session_id', sessionId)
     }
     return sessionId
   }
   ```

3. **Supabase 쿼리 함수**
   ```tsx
   // src/lib/supabase/survey.ts
   export async function submitSurveyResponse(responses: SurveyResponses) {
     const sessionId = getOrCreateSessionId()
     // ... 중복 체크 및 저장
   }
   ```

### Phase 3: 설문 UI 구현 (2시간)

1. **SurveyFloatingButton 컴포넌트**
   ```tsx
   // src/components/survey/SurveyFloatingButton.tsx
   // 우측 하단 플로팅 버튼 (로그인 버튼 위에 배치)
   ```

2. **SurveyModal 컴포넌트**
   ```tsx
   // src/components/survey/SurveyModal.tsx
   // 다단계 폼 (react-hook-form 사용)
   ```

3. **설문 표시 로직**
   ```tsx
   // src/hooks/useSurveyTrigger.ts
   export function useSurveyTrigger() {
     const [shouldShow, setShouldShow] = useState(false)

     useEffect(() => {
       // 로컬스토리지 체크: 이미 응답했는지
       const hasResponded = localStorage.getItem('survey_completed')
       if (hasResponded) return

       // 첫 방문 시간 기록
       const firstVisit = localStorage.getItem('first_visit_time')
       if (!firstVisit) {
         localStorage.setItem('first_visit_time', Date.now().toString())
       }

       // 5분 경과 후 표시
       const elapsed = Date.now() - parseInt(firstVisit)
       if (elapsed > 5 * 60 * 1000) {
         setShouldShow(true)
       }
     }, [])

     return { shouldShow, dismiss: () => setShouldShow(false) }
   }
   ```

### Phase 4: Hero.tsx 통합 (30분)

```tsx
// src/pages/new-landing/components/Hero.tsx
import { SurveyFloatingButton } from '@/components/survey/SurveyFloatingButton'
import { useSurveyTrigger } from '@/hooks/useSurveyTrigger'

export const Hero: React.FC = () => {
  const { shouldShow, dismiss } = useSurveyTrigger()

  return (
    <section>
      {/* 기존 코드... */}

      {/* 설문 플로팅 버튼 */}
      {shouldShow && <SurveyFloatingButton onDismiss={dismiss} />}
    </section>
  )
}
```

---

## 4. UX 고려사항

### 4.1 비침습적 설계
- ✅ 플로팅 버튼 방식: 사용자가 선택적으로 참여
- ✅ "나중에" 버튼: 강요하지 않음
- ✅ "다시 보지 않기" 옵션: 귀찮아하는 사용자 배려
- ❌ 모달 자동 팝업: 첫 방문 1분 이내 표시 금지

### 4.2 모바일 최적화
- 설문 폼: 스크롤 가능한 세로 레이아웃
- 큰 터치 영역 (별점, 체크박스)
- 키보드 입력 시 화면 자동 스크롤

### 4.3 인센티브 고려 (선택)
- "설문 참여자 추첨: 스타벅스 기프티콘 10명"
- 이메일 수집 필드 추가 (선택 사항)
- RLS 정책으로 개인정보 보호

---

## 5. 데이터 분석

### 5.1 Supabase 대시보드 쿼리

```sql
-- 전체 응답 수
SELECT COUNT(*) FROM survey_responses;

-- 만족도 평균
SELECT
  AVG((responses->'ratings'->>'overall')::int) as avg_overall,
  AVG((responses->'ratings'->>'search_convenience')::int) as avg_search,
  AVG((responses->'ratings'->>'map_usefulness')::int) as avg_map
FROM survey_responses;

-- 발견 경로 분포
SELECT
  responses->>'discovery_channel' as channel,
  COUNT(*) as count
FROM survey_responses
GROUP BY channel
ORDER BY count DESC;

-- 재방문 의향
SELECT
  responses->>'revisit_intention' as intention,
  COUNT(*) as count
FROM survey_responses
GROUP BY intention;
```

### 5.2 관리자 페이지 통합 (선택)
- AdminPage에 "설문 응답 분석" 탭 추가
- 차트 라이브러리 (recharts): 만족도 그래프
- 개선 의견 텍스트 목록 표시

---

## 6. 타임라인

| 날짜 | 작업 | 예상 시간 |
|------|------|----------|
| Day 1 | 베타 배지 구현 + 환경 변수 설정 | 1h |
| Day 2 | DB 마이그레이션 + Supabase 쿼리 작성 | 1.5h |
| Day 3 | 설문 UI 컴포넌트 구현 (플로팅 버튼 + 모달) | 3h |
| Day 4 | 트리거 로직 + Hero.tsx 통합 | 1h |
| Day 5 | 테스트 + 디버깅 (모바일/데스크톱) | 2h |
| **총** | | **8.5시간** |

---

## 7. 체크리스트

### 베타 배지
- [ ] `.env`에 `VITE_IS_BETA=true`, `VITE_BETA_END_DATE` 설정
- [ ] `BetaBadge.tsx` 컴포넌트 생성
- [ ] `Hero.tsx` 로고 옆에 배지 추가
- [ ] 모바일 반응형 확인

### 설문조사
- [ ] `survey_responses` 테이블 마이그레이션
- [ ] `sessionId` 생성 유틸 작성
- [ ] `submitSurveyResponse()` Supabase 쿼리 작성
- [ ] `SurveyFloatingButton.tsx` 컴포넌트 생성
- [ ] `SurveyModal.tsx` 폼 구현 (react-hook-form)
- [ ] `useSurveyTrigger.ts` 훅 작성
- [ ] `Hero.tsx`에 설문 버튼 통합
- [ ] 중복 응답 방지 테스트
- [ ] 모바일 UI 테스트
- [ ] 응답 데이터 Supabase에서 확인

### 선택사항
- [ ] 관리자 페이지 설문 분석 탭
- [ ] 인센티브 안내 문구 추가
- [ ] 설문 완료 후 감사 메시지 애니메이션

---

## 8. 주의사항

1. **개인정보 보호**
   - 비로그인 사용자 응답 가능 (익명성 보장)
   - `user_id`는 NULL 허용
   - RLS 정책: 모든 사용자가 자신의 응답만 볼 수 있음

2. **성능**
   - 설문 모달: lazy loading으로 초기 번들 크기 최소화
   - 로컬스토리지 활용으로 서버 부하 감소

3. **A/B 테스트 고려**
   - 플로팅 버튼 vs 자동 팝업 효과 비교
   - 표시 시점(3분 vs 5분 vs 공고 3개 클릭) 실험

4. **베타 종료 후**
   - `VITE_IS_BETA=false`로 변경 후 재배포
   - 설문 버튼은 유지 (정식 오픈 후에도 피드백 수집 가능)

---

## 9. 추천 구현 순서

**최소 기능 (MVP)**:
1. 베타 배지만 먼저 구현 (30분)
2. 간단한 Google Forms 링크 버튼 (1시간)
   - 플로팅 버튼 클릭 시 새 탭에서 구글 폼 열기
   - DB 구축 없이 빠르게 피드백 수집

**풀 기능**:
1. 위 Phase 1~4 전체 구현 (8.5시간)
2. 사내 데이터 분석 가능

---

## 참고: Google Forms 간단 연동 (MVP)

DB 구축이 부담스러우면:

```tsx
// src/components/survey/SurveyFloatingButton.tsx
export function SurveyFloatingButton() {
  const handleClick = () => {
    window.open('https://forms.gle/YOUR_FORM_ID', '_blank')
    localStorage.setItem('survey_shown', 'true')
  }

  return (
    <button
      onClick={handleClick}
      className="fixed bottom-20 right-4 z-30 bg-orange-500 text-white px-4 py-2 rounded-full shadow-lg hover:bg-orange-600"
    >
      💬 베타 설문조사
    </button>
  )
}
```

**장점**: 즉시 구현 가능, Google Sheets 자동 저장
**단점**: 사용자 경험이 덜 seamless, 중복 응답 방지 어려움
