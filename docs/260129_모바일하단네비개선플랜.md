# 모바일 하단 네비바 개선 플랜

**작성일**: 2026-01-29
**목표**: 채팅/즐겨찾기를 플로팅으로 이동, 교원연수 기능 추가

---

## 1. 현재 레이아웃

```
┌─────────────────────────────────────────────────────────────────┐
│                          지도 영역                               │
│                                                                 │
│                                          ┌──────┐              │
│                                          │ 설문 │              │
│                                          │ 참여 │              │
│                                          └──────┘              │
│                                             ◎  (현재위치)       │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│ 주변공고 5개                                      │ 더보기 ∧   │
├──────────┬─────────┬────────┬────────┬────────┬─────────────────┤
│    📍    │   👤    │   💬   │   ⭐   │   +    │       📄       │
│ 공고보기 │구직자   │  채팅  │즐겨찾기│구직등록│    공고등록     │
│         │  보기   │        │        │        │                 │
└──────────┴─────────┴────────┴────────┴────────┴─────────────────┘
```

**파일**: `src/components/mobile/MobileRegisterNav.tsx`

---

## 2. 변경 후 레이아웃

```
┌─────────────────────────────────────────────────────────────────┐
│                          지도 영역                               │
│                                                                 │
│                                          ┌──────┐              │
│                                          │ 설문 │              │
│                                          │ 참여 │              │
│                                          └──────┘              │
│                                             ◎  (현재위치)       │
│                                            ┌──┐                │
│                                            │💬│ (채팅 플로팅)   │
│                                            └──┘                │
│                                            ┌──┐                │
│                                            │⭐│ (즐겨찾기 플로팅)│
│                                            └──┘                │
├─────────────────────────────────────────────────────────────────┤
│ 주변공고 5개                                      │ 더보기 ∧   │
├──────────┬─────────┬───────────┬────────┬────────┬─────────────┤
│    📍    │   👤    │    🎓     │   +    │   📄   │     🎓      │
│ 공고보기 │구직자   │  교원연수  │구직등록│공고등록│   교원연수   │
│         │  보기   │ 강사보기   │        │        │  강사등록   │
└──────────┴─────────┴───────────┴────────┴────────┴─────────────┘
```

---

## 3. 수정 파일 및 상세 계획

### 3.1 `src/components/mobile/MobileRegisterNav.tsx`

#### Props 추가

```typescript
interface MobileRegisterNavProps {
  // 기존 props...
  showJobLayer: boolean;
  showSeekerLayer: boolean;
  showInstructorLayer: boolean;  // NEW
  onJobLayerToggle: () => void;
  onSeekerLayerToggle: () => void;
  onInstructorLayerToggle: () => void;  // NEW
  onJobSeekerRegister: () => void;
  onJobPostRegister: () => void;
  onInstructorRegister: () => void;  // NEW
  isLoggedIn: boolean;
  // 채팅, 즐겨찾기 props 제거 (플로팅으로 이동)
}
```

#### Import 변경

```typescript
// 기존 lucide-react 유지 + Solar Icons 추가
import { MapPin, User, Plus, FileText } from 'lucide-react';
import PresentationGraph from '@solar-icons/react/csr/business/PresentationGraph';

// 교원연수 핑크 컬러
const INSTRUCTOR_COLOR = '#F9A8D4';
const INSTRUCTOR_ACTIVE_COLOR = '#EC4899';
```

#### 버튼 구조 (6개)

| 순서 | 아이콘 | 라벨 | 색상 |
|------|--------|------|------|
| 1 | MapPin (lucide) | 공고보기 | 스카이블루 #4facfe |
| 2 | User (lucide) | 구직자보기 | 스카이블루 #4facfe |
| 3 | PresentationGraph (solar) | 교원연수<br>강사보기 | 핑크 #F9A8D4 |
| 4 | Plus (lucide) | 구직등록 | 회색 #6B7280 |
| 5 | FileText (lucide) | 공고등록 | 회색 #6B7280 |
| 6 | PresentationGraph (solar) | 교원연수<br>강사등록 | 핑크 #F9A8D4 |

#### 버튼 코드 예시

```tsx
{/* 3. 교원연수 강사보기 토글 */}
<button
  onClick={onInstructorLayerToggle}
  className="flex flex-col items-center justify-center flex-1 h-full transition-colors"
  aria-label={`교원연수 강사보기 ${showInstructorLayer ? '켜짐' : '꺼짐'}`}
>
  <PresentationGraph
    size={20}
    color={showInstructorLayer ? INSTRUCTOR_ACTIVE_COLOR : '#9CA3AF'}
  />
  <span
    className="text-[10px] mt-0.5 font-medium leading-tight text-center"
    style={{ color: showInstructorLayer ? INSTRUCTOR_ACTIVE_COLOR : '#9CA3AF' }}
  >
    교원연수<br />강사보기
  </span>
</button>

{/* 6. 교원연수 강사등록 */}
<button
  onClick={onInstructorRegister}
  className="flex flex-col items-center justify-center flex-1 h-full transition-colors"
  aria-label="교원연수 강사등록"
>
  <PresentationGraph size={20} color="#6B7280" />
  <span className="text-[10px] mt-0.5 font-medium text-gray-500 leading-tight text-center">
    교원연수<br />강사등록
  </span>
</button>
```

---

### 3.2 `src/pages/new-landing/components/Hero.tsx`

#### 플로팅 버튼 추가 (모바일 전용)

위치: 설문참여 버튼 아래, 현재위치 버튼 아래

```tsx
{/* 모바일 채팅 플로팅 버튼 */}
<button
  onClick={() => {
    setComingSoonFeature('채팅');
    setIsComingSoonOpen(true);
  }}
  className="md:hidden absolute right-4 z-20 w-11 h-11 bg-white rounded-full shadow-lg
    flex items-center justify-center border border-gray-200 hover:bg-gray-50 transition-colors"
  style={{ bottom: 'calc(env(safe-area-inset-bottom) + 180px)' }}
  aria-label="채팅"
>
  <MessageCircle size={22} strokeWidth={1.8} className="text-gray-500" />
</button>

{/* 모바일 즐겨찾기 플로팅 버튼 */}
<button
  onClick={() => {
    setComingSoonFeature('즐겨찾기');
    setIsComingSoonOpen(true);
  }}
  className="md:hidden absolute right-4 z-20 w-11 h-11 bg-white rounded-full shadow-lg
    flex items-center justify-center border border-gray-200 hover:bg-gray-50 transition-colors"
  style={{ bottom: 'calc(env(safe-area-inset-bottom) + 124px)' }}
  aria-label="즐겨찾기"
>
  <Star size={22} strokeWidth={1.8} className="text-gray-500" />
</button>
```

#### MobileRegisterNav Props 전달

```tsx
<MobileRegisterNav
  showJobLayer={showJobLayer}
  showSeekerLayer={showSeekerLayer}
  showInstructorLayer={cascadingFilter.primary === '교원연수'}
  onJobLayerToggle={() => setShowJobLayer(prev => !prev)}
  onSeekerLayerToggle={() => setShowSeekerLayer(prev => !prev)}
  onInstructorLayerToggle={() => {
    if (cascadingFilter.primary === '교원연수') {
      setCascadingFilter({ primary: null, secondary: null, tertiary: null });
    } else {
      setCascadingFilter({ primary: '교원연수', secondary: null, tertiary: null });
    }
  }}
  onJobSeekerRegister={() => {
    if (!user) {
      setPendingAction('register');
      setAuthModalInitialTab('login');
      setIsAuthModalOpen(true);
      return;
    }
    setLocationPickerType('teacher');
    setIsLocationPickerOpen(true);
  }}
  onJobPostRegister={() => {
    if (!user) {
      setPendingAction('jobPost');
      setAuthModalInitialTab('login');
      setIsAuthModalOpen(true);
      return;
    }
    setLocationPickerType('jobPosting');
    setIsLocationPickerOpen(true);
  }}
  onInstructorRegister={() => {
    setIsInstructorModalOpen(true);
  }}
  isLoggedIn={!!user}
/>
```

---

## 4. 플로팅 버튼 위치 계산

```
┌────────────────────────────┐
│                            │
│          설문참여 버튼      │ top: 16px
│                            │
│          ◎ 현재위치        │ 설문 아래
│                            │
│          💬 채팅           │ bottom: 180px (safe-area + 180)
│                            │
│          ⭐ 즐겨찾기        │ bottom: 124px (safe-area + 124)
│                            │
├────────────────────────────┤
│ 주변공고 바                 │ bottom: 64px (h-16)
├────────────────────────────┤
│ 하단 네비바 (h-16)          │ bottom: 0
└────────────────────────────┘
```

---

## 5. 스타일 일관성

| 요소 | 스타일 |
|------|--------|
| 플로팅 버튼 크기 | w-11 h-11 (44px) |
| 플로팅 버튼 배경 | bg-white rounded-full |
| 플로팅 버튼 그림자 | shadow-lg |
| 플로팅 버튼 테두리 | border border-gray-200 |
| 플로팅 아이콘 크기 | size={22} |
| 플로팅 아이콘 색상 | text-gray-500 |
| 네비 버튼 아이콘 크기 | size={20} |
| 네비 버튼 텍스트 | text-[10px] |

---

## 6. 구현 체크리스트

- [ ] MobileRegisterNav.tsx 수정
  - [ ] 채팅, 즐겨찾기 버튼 제거
  - [ ] 교원연수 강사보기 버튼 추가
  - [ ] 교원연수 강사등록 버튼 추가
  - [ ] Solar Icons import
  - [ ] Props 타입 수정

- [ ] Hero.tsx 수정
  - [ ] 채팅 플로팅 버튼 추가
  - [ ] 즐겨찾기 플로팅 버튼 추가
  - [ ] MobileRegisterNav props 업데이트

- [ ] 빌드 확인
- [ ] 모바일 테스트
